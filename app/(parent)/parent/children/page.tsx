import { ParentChildrenClient } from "@/components/parent/ParentChildrenClient"

export const metadata = { title: "Mes enfants | KLASSCI" }

export default function ParentChildrenPage() {
  return <ParentChildrenClient />
}
