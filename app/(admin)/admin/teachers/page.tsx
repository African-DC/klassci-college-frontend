import { TeachersPageClient } from "@/components/admin/teachers/TeachersPageClient"

export const metadata = { title: "Enseignants | KLASSCI" }

export default function TeachersPage() {
  return <TeachersPageClient />
}
